// Generated by CoffeeScript 1.6.3
(function() {
  var argv, async, cn, fs, hex2base64url, pem;

  pem = require('pem');

  async = require('async');

  fs = require('fs');

  argv = process.argv.slice(2);

  cn = argv[0] || 'localhost';

  hex2base64url = function(hex) {
    var b64;
    b64 = new Buffer(hex, 'hex').toString('base64');
    return b64.replace(/\=/g, '').replace(/\+/g, '-').replace(/\//g, '_');
  };

  pem.createCertificate({
    days: 1,
    selfSigned: true,
    commonName: cn
  }, function(err, keys) {
    if (err) {
      console.log(err);
      return;
    }
    return async.parallel([
      function(cb) {
        return pem.getModulus(keys.certificate, cb);
      }, function(cb) {
        return pem.getFingerprint(keys.certificate, cb);
      }, function(cb) {
        return fs.writeFile("" + cn + "-key.pem", keys.clientKey, cb);
      }, function(cb) {
        return fs.writeFile("" + cn + ".pem", keys.certificate, cb);
      }
    ], function(er, results) {
      var cert, fing, json, modulus, posh;
      modulus = hex2base64url(results[0].modulus);
      fing = results[1].fingerprint.replace(/:/g, '');
      fing = hex2base64url(fing);
      cert = keys.certificate.replace(/-----[^\n]+\n?/gm, '');
      cert = cert.replace(/\n/g, '');
      posh = {
        keys: [
          {
            kty: "RSA",
            kid: "" + cn + ":" + fing,
            n: modulus,
            e: "AQAB",
            x5c: cert
          }
        ]
      };
      json = JSON.stringify(posh);
      return fs.writeFile("posh._xmpp._tcp.json", json, function(er) {
        if (er) {
          return console.log(er);
        }
      });
    });
  });

}).call(this);
